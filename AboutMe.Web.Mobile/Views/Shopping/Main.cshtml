@model IEnumerable<AboutMe.Domain.Entity.Product.SP_PRODUCT_MAIN_SEL_Result>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AboutMeLayoutA.cshtml";
}
@section Head{
    @{ Html.RenderPartial("~/Views/AboutMePartial/_Head.cshtml");}
    <link href="/aboutCom/css/shopping.css" rel="stylesheet" />
}


<!--header-->
@section Header{

}
<!--//header-->
@section Scripts{

    <script language="javascript">

          $(function () {
              //ajaxSelectCateCodeJavascriptInit();
          });

    </script>

}

<!--container-->
@section Body{

    <div class="wrap display">
        @{ Html.RenderPartial("~/Views/AboutMePartial/_Header.cshtml");}
        <!--container-->
        <div class="container">
            <!--전시메인배너-->
            <div class="display_bnr">
                <div class="nav">
                    <ul></ul>
                </div>
                <div class="slider">
                    <img src="/aboutCom/images/sample/event_ban2.jpg" class="dummy" alt="" />
                    <ul>
                        <li><img src="/aboutCom/images/sample/event_ban2.jpg" alt="" /></li>
                        <li><img src="/aboutCom/images/sample/event_ban1.jpg" alt="" /></li>
                        <li><img src="/aboutCom/images/sample/event_ban2.jpg" alt="" /></li>
                    </ul>
                </div>
            </div>
            <!--//전시메인배너-->
            <!--best sellers-->
            <div class="best_sellers">
                <img src="/aboutCom/images/shopping/bnr_bestseller.jpg" class="imgtit" alt="Best Sellers 어바웃미 핫 아이템 베스트셀러 .10" />
                <ul class="tabs tabtyped">
                    <li class="cnt"><a href="#Beauty">뷰티</a></li>
                    <li><a href="#Health">헬스</a></li>
                    <li><a href="#Etc">셀렉샵</a></li>
                </ul>
                <!--뷰티-->
                <div id="Beauty" class="tab_content" style="display:block">
                    <ul class="product_list">
                        @{ var i = 1; }
                        @foreach (var item in ViewData["CateCodeB"] as List<AboutMe.Domain.Entity.Product.SP_PRODUCT_MAIN_SEL_Result>)
                        {
                            string imgUrl1 = ViewBag.PRODUCT_PATH + "R270_" + item.MAIN_IMG;
                            <li>
                                <a href="/shopping/detail?pcode=@item.P_CODE">
                                    <span class="photo"><img src="@imgUrl1" alt="" /><em class="ranking">@i</em></span>
                                    <span class="detail">
                                        @if (!string.IsNullOrEmpty(item.ICON_YN))
                                        {
                                            if (item.ICON_YN.IndexOf('B') != -1)
                                            { <text> <span class="ico_best">BEST</span></text> }
                                            if (item.ICON_YN.IndexOf('S') != -1)
                                            { <text> <span class="ico_soldout">SOLD OUT</span></text> }
                                            if (item.ICON_YN.IndexOf('N') != -1)
                                            { <text> <span class="ico_new">NEW</span></text> }
                                            if (item.ICON_YN.IndexOf('C') != -1)
                                            { <text> <span class="ico_custom ">커스텀</span></text> }
                                        }
                                        @if (!string.IsNullOrEmpty(item.PMO_PRODUCT_CATEGORY))
                                        {
                                            if (item.PMO_PRODUCT_CATEGORY == "01")
                                            { <text> <span class="ico_set">SET</span></text> }
                                            if (item.PMO_PRODUCT_CATEGORY == "02")
                                            { <text> <span class="ico_1_1">1+1</span></text> }
                                            if (item.PMO_PRODUCT_CATEGORY == "03")
                                            { <text> <span class="ico_time">TIME</span></text> }
                                        }
                                        @if (!string.IsNullOrEmpty(item.COUPON_CODE))
                                        {
                                            <text> <span class="ico_coupon">COUPON</span></text>
                                        }
                                        
                                        <span class="tit">@(new HtmlString(item.P_NAME))</span>
                                        <span class="stit">@(new HtmlString(item.P_SUB_TITLE))</span>
                                        <span class="price"><del>@string.Format("{0:#,##0}", item.SELLING_PRICE)원</del><strong>@string.Format("{0:#,##0}", item.RESULT_PRICE)</strong>원</span>
                                    </span>
                                </a>
                                <a href="javascript:Cart_Product_Input('@item.P_CODE','1');" class="btn_cart"></a>
                            </li>
                          i++;
                        }
                    </ul>
                </div>
                <!--//뷰티-->
                <!--헬스-->
                <div id="Health" class="tab_content">
                    <ul class="product_list">
                        @{ i = 1; }
                        @foreach (var item in ViewData["CateCodeH"] as List<AboutMe.Domain.Entity.Product.SP_PRODUCT_MAIN_SEL_Result>)
                        {
                            string imgUrl1 = ViewBag.PRODUCT_PATH + "R270_" + item.MAIN_IMG;
                            <li>
                                <a href="/shopping/detail?pcode=@item.P_CODE">
                                    <span class="photo"><img src="@imgUrl1" alt="" /><em class="ranking">@i</em></span>
                                    <span class="detail">
                                        @if (!string.IsNullOrEmpty(item.ICON_YN))
                                        {
                                            if (item.ICON_YN.IndexOf('B') != -1)
                                            { <text> <span class="ico_best">BEST</span></text> }
                                            if (item.ICON_YN.IndexOf('S') != -1)
                                            { <text> <span class="ico_soldout">SOLD OUT</span></text> }
                                            if (item.ICON_YN.IndexOf('N') != -1)
                                            { <text> <span class="ico_new">NEW</span></text> }
                                            if (item.ICON_YN.IndexOf('C') != -1)
                                            { <text> <span class="ico_custom ">커스텀</span></text> }
                                        }
                                        @if (!string.IsNullOrEmpty(item.PMO_PRODUCT_CATEGORY))
                                        {
                                            if (item.PMO_PRODUCT_CATEGORY == "01")
                                            { <text> <span class="ico_set">SET</span></text> }
                                            if (item.PMO_PRODUCT_CATEGORY == "02")
                                            { <text> <span class="ico_1_1">1+1</span></text> }
                                            if (item.PMO_PRODUCT_CATEGORY == "03")
                                            { <text> <span class="ico_time">TIME</span></text> }
                                        }
                                        @if (!string.IsNullOrEmpty(item.COUPON_CODE))
                                        {
                                            <text> <span class="ico_coupon">COUPON</span></text>
                                        }
                                        <span class="tit">@(new HtmlString(item.P_NAME))</span>
                                        <span class="stit">@(new HtmlString(item.P_SUB_TITLE))</span>
                                        <span class="price"><del>@string.Format("{0:#,##0}", item.SELLING_PRICE)원</del><strong>@string.Format("{0:#,##0}", item.RESULT_PRICE)</strong>원</span>
                                    </span>
                                </a>
                                <a href="javascript:Cart_Product_Input('@item.P_CODE','1');" class="btn_cart"></a>
                            </li>
                            i++;
                        }
                    </ul>
                </div>
                <!--//헬스-->
                <!--셀렉샵-->
                <div id="Etc" class="tab_content">
                    <ul class="product_list">
                        @{ i = 1; }
                        @foreach (var item in ViewData["CateCodeS"] as List<AboutMe.Domain.Entity.Product.SP_PRODUCT_MAIN_SEL_Result>)
                        {
                            string imgUrl1 = ViewBag.PRODUCT_PATH + "R270_" + item.MAIN_IMG;
                            <li>
                                <a href="/shopping/detail?pcode=@item.P_CODE">
                                    <span class="photo"><img src="@imgUrl1" alt="" /><em class="ranking">@i</em></span>
                                    <span class="detail">
                                        @if (!string.IsNullOrEmpty(item.ICON_YN))
                                        {
                                            if (item.ICON_YN.IndexOf('B') != -1)
                                            { <text> <span class="ico_best">BEST</span></text> }
                                            if (item.ICON_YN.IndexOf('S') != -1)
                                            { <text> <span class="ico_soldout">SOLD OUT</span></text> }
                                            if (item.ICON_YN.IndexOf('N') != -1)
                                            { <text> <span class="ico_new">NEW</span></text> }
                                            if (item.ICON_YN.IndexOf('C') != -1)
                                            { <text> <span class="ico_custom ">커스텀</span></text> }
                                        }
                                        @if (!string.IsNullOrEmpty(item.PMO_PRODUCT_CATEGORY))
                                        {
                                            if (item.PMO_PRODUCT_CATEGORY == "01")
                                            { <text> <span class="ico_set">SET</span></text> }
                                            if (item.PMO_PRODUCT_CATEGORY == "02")
                                            { <text> <span class="ico_1_1">1+1</span></text> }
                                            if (item.PMO_PRODUCT_CATEGORY == "03")
                                            { <text> <span class="ico_time">TIME</span></text> }
                                        }
                                        @if (!string.IsNullOrEmpty(item.COUPON_CODE))
                                        {
                                            <text> <span class="ico_coupon">COUPON</span></text>
                                        }
                                        <span class="tit">@(new HtmlString(item.P_NAME))</span>
                                        <span class="stit">@(new HtmlString(item.P_SUB_TITLE))</span>
                                        <span class="price"><del>@string.Format("{0:#,##0}", item.SELLING_PRICE)원</del><strong>@string.Format("{0:#,##0}", item.RESULT_PRICE)</strong>원</span>
                                    </span>
                                </a>
                                <a href="javascript:Cart_Product_Input('@item.P_CODE','1');" class="btn_cart"></a>
                            </li>
                            i++;
                        }
                    </ul>
                </div>
                <!--//셀렉샵-->
            </div>
            <!--//best sellers-->

        </div>
        <!--//container-->
        @{ Html.RenderPartial("~/Views/AboutMePartial/_Footer.cshtml");}
    </div>
}
<!--footer-->
@section Footer{

}
<!--//footer-->
